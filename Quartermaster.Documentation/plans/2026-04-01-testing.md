# Testing Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add unit tests for the 6 critical business logic areas not yet covered by the existing 201 validator/sanitization tests.

**Architecture:** All tests that need database access require a test database setup. Two approaches: (A) mock repositories using interfaces, or (B) use an in-memory SQLite or real MySQL test database. Since the repositories use LinqToDB directly (not interfaces), option B is more practical — use a test MySQL database with the migration runner. For pure logic tests (like chapter resolution), in-memory data structures work.

**Tech Stack:** TUnit, existing test project, LinqToDB, FluentMigrator

---

## Existing Coverage

| Area | Tests | Status |
|---|---|---|
| Input Validation (18 validators) | 173 | ✅ Done |
| HTML Sanitization (Strict + Standard) | 18 | ✅ Done |
| Markdown Service | 10 | ✅ Done |
| **Business logic** | **0** | **Needs tests** |

---

## Test Plan: 6 Test Suites, ~64 Test Cases

### Task 1: Test Infrastructure — TestDatabaseFixture

Before writing business logic tests, create a shared test fixture that:
1. Creates a test MySQL database (`quartermaster_test`)
2. Runs FluentMigrator to create schema
3. Provides a fresh `DbContext` per test (with transaction rollback or truncation between tests)
4. Cleans up after all tests

This avoids mocking the data layer and tests the actual SQL queries.

**Files:**
- Create: `Quartermaster.Server.Tests/Infrastructure/TestDatabaseFixture.cs`

---

### Task 2: ChapterRepository Tests (~14 cases)

**File:** `Quartermaster.Server.Tests/Chapters/ChapterRepositoryTests.cs`

**GetAncestorChain:**
- [x] Single node (no parent) → returns just itself
- [x] Three-level hierarchy → returns [child, parent, grandparent]
- [x] Circular reference protection (node with ParentChapterId == own Id) → doesn't loop forever
- [x] Chain order is child-to-root

**GetDescendantIds:**
- [x] Leaf node → returns just its own ID
- [x] Two-level tree → returns parent + child IDs
- [x] Multi-level tree → returns all descendant IDs
- [x] Self-referencing protection

**FindByExternalCodeAndParent:**
- [x] Exact match on code + parent → returns chapter
- [x] Code match, wrong parent → returns null
- [x] Parent match, wrong code → returns null
- [x] Null parent (root chapter match) → returns chapter
- [x] Non-existent code → returns null

**Seed data:** Create a 3-level chapter hierarchy (Bund → LV → Bezirk → Kreis) with ExternalCodes.

---

### Task 3: OptionRepository.ResolveValue Tests (~9 cases)

**File:** `Quartermaster.Server.Tests/Options/OptionRepositoryTests.cs`

- [x] No chapterId → returns global value
- [x] No global, no chapter value → returns null
- [x] Chapter-level value at target chapter → returns it
- [x] Chapter-level value at parent → inherits
- [x] Chapter-level value overrides parent value
- [x] Global value as fallback when no chapter match
- [x] Deepest ancestor match wins (most specific)
- [x] Identifier not defined → returns null
- [x] Empty string value vs null value

**Seed data:** 3-level chapter hierarchy + option definitions + values at different levels.

---

### Task 4: MotionRepository.TryAutoResolve Tests (~11 cases)

**File:** `Quartermaster.Server.Tests/Motions/MotionRepositoryTests.cs`

- [x] Motion not found → returns false
- [x] Motion not Pending → returns false
- [x] No officers in chapter → returns false
- [x] Approve majority reached (3 of 5 officers approve) → Approved
- [x] Deny majority reached (3 of 5 deny) → Rejected
- [x] No majority (2 approve, 2 deny, 1 abstain) → returns false
- [x] Even officer count: 2 of 4 approve → not enough (need 3)
- [x] Even officer count: 3 of 4 approve → Approved
- [x] Linked MembershipApplication status updated on approval
- [x] Linked DueSelection status updated on rejection
- [x] Audit log entry created on resolution

**Seed data:** Chapter with officers, motion with votes.

---

### Task 5: MemberImportService Tests (~13 cases)

**File:** `Quartermaster.Server.Tests/Members/MemberImportServiceTests.cs`

**ResolveChapter (extracted or tested via import):**
- [x] Kreis with correct Bezirk and LV → resolves to Kreis chapter
- [x] Bezirk with correct LV → resolves to Bezirk chapter
- [x] LV only → resolves to LV chapter
- [x] All empty → returns null
- [x] Kreis with wrong parent chain → returns null
- [x] ExternalCode match but wrong hierarchy → returns null

**ImportFromFile:**
- [x] Valid CSV creates new members
- [x] Existing member (by MemberNumber) is updated
- [x] Updated member preserves UserId
- [x] Invalid row logged as error, doesn't stop import
- [x] Import log has correct statistics (new/updated/error counts)
- [x] File hash (SHA256) computed correctly
- [x] Boolean fields parsed ("1" → true, "0" → false)

**Note:** These tests need a sample CSV file. Create `Quartermaster.Server.Tests/TestData/sample_import.csv` with a few rows.

---

### Task 6: EmailService Tests (~10 cases)

**File:** `Quartermaster.Server.Tests/Email/EmailServiceTests.cs`

- [x] Template override used when provided
- [x] Template loaded from option identifier when no override
- [x] Error returned when no template content available
- [x] Manual addresses: split, validated, deduplicated
- [x] Manual addresses: invalid emails (no @) skipped
- [x] Chapter target: enqueues emails for members in chapter + descendants
- [x] Members without email addresses skipped
- [x] EmailLog entries created as "Pending" for each
- [x] Returns correct count
- [x] Source entity info (type + id) preserved in logs

**Approach:** Use a mock/fake for the `Channel<EmailMessage>` — create a real unbounded channel in the test, inject it, then read from it to verify what was enqueued. No SMTP or mail system setup needed. Repository dependencies use the real test database.

---

### Task 7: UserRepository.SupplementDefaults Tests (~7 cases)

**File:** `Quartermaster.Server.Tests/Users/UserRepositoryTests.cs`

- [x] Null settings → no user created
- [x] Empty username → no user created
- [x] Empty password → no user created
- [x] New user: created with hashed password
- [x] Existing user: not duplicated
- [x] Permissions granted: CreateUser + CreateChapter
- [x] Idempotent: second call doesn't duplicate permissions

---

## Summary

| Task | Test Suite | Cases | Priority |
|---|---|---|---|
| 1 | Test Infrastructure | — | Foundation |
| 2 | ChapterRepository | 14 | High (used by import, options, email) |
| 3 | OptionRepository.ResolveValue | 9 | High (used by email, templates) |
| 4 | MotionRepository.TryAutoResolve | 11 | High (core voting logic) |
| 5 | MemberImportService | 13 | High (daily automated process) |
| 6 | EmailService | 10 | Medium (new system) |
| 7 | UserRepository.SupplementDefaults | 7 | Low (simple, rarely changes) |
| **Total** | | **~64** | |

## Execution Notes

- Tasks 2-4 are the highest value — they test the core business logic that's hard to verify manually
- Task 5 (import) provides the most protection against regressions since imports run daily
- Task 1 (test infrastructure) is prerequisite for all others
- Tests should use a separate `quartermaster_test` database to avoid interfering with dev data
- Each test should be independent — seed its own data and clean up after
