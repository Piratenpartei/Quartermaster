{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "DatabaseSettings": {
    "ConnectionString": "server=<host>;user id=<user>;password=<password>;database=quartermaster;pooling=true;max pool size=20;min pool size=5;"
  },
  "ForwardedHeaders": {
    "//comment": "Both lists empty by default ⇒ X-Forwarded-* headers are ignored and Connection.RemoteIpAddress is authoritative. Only add entries when running behind a reverse proxy you trust to set X-Forwarded-For correctly.",
    "KnownProxies": [],
    "KnownNetworks": []
  }
}
